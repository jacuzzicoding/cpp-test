public class Engine extends Vehicle { //Engine is a subclass of Vehicle
    public Engine(String make, String model, int year, int fuelCapacity) {
        super(make, model, year, fuelCapacity);
    }
    
}
