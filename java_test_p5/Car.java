public class Car extends Vehicle {
    //call the constructor of the parent class
    public Car(String make, String model, int year, int fuelCapacity) {
        super(make, model, year, fuelCapacity);
    }
}
